export interface navbarLink {
    name: string;
    adrress: string
}
export interface featureInter {
    image: any;
    title: string;
    description: string;
    link: string
}

export interface Brand {
    brand: string;
    img: string;
}